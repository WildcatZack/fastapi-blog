# Milestone 1A (API only)
APP_ENV=development

# Choose the port the app listens to *inside* the container.
# If omitted, defaults to 8000.
# PORT=8081

# Choose the port exposed on your host (the port you visit in the browser).
# If omitted, defaults to 8000.
# HOST_PORT=18000
